<%= form_for(therapy_reason) do |f| %>
  <% if therapy_reason.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(therapy_reason.errors.count, "error") %> prohibited this therapy_reason from being saved:</h2>

      <ul>
      <% therapy_reason.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
