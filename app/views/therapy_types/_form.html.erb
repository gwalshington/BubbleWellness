<%= form_for(therapy_type) do |f| %>
  <% if therapy_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(therapy_type.errors.count, "error") %> prohibited this therapy_type from being saved:</h2>

      <ul>
      <% therapy_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
