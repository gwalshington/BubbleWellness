<%= form_for(therapy_format) do |f| %>
  <% if therapy_format.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(therapy_format.errors.count, "error") %> prohibited this therapy_format from being saved:</h2>

      <ul>
      <% therapy_format.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
